{
  "buildCommand": "cd web && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "web/dist",
  "framework": "astro",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
