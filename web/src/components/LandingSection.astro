---
export interface Props {
  id: string;
  className?: string;
  bgColor?: string;
}

const { id, className = "", bgColor = "bg-white" } = Astro.props;
---

<section id={id} class={`py-16 ${bgColor} ${className}`}>
  <div class="container mx-auto px-4">
    <slot />
  </div>
</section>
